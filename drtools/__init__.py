"""drtools — deterministic toolbox for the dr-agent dimension-reduction agent.

Contains no LLM code. Every decision the agent makes is expressed as a call into
this package; every number the agent reports comes out of it.
"""

__version__ = "0.1.0"
